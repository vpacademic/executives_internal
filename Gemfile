source "https://rubygems.org"

gem "jekyll", "~> 4.3"
gem "just-the-docs"

# Pin sass-embedded to a version compatible with macOS 13 (local dev only)
gem "sass-embedded", "~> 1.77.1"  # update from ~> 1.76.0
ruby "3.1.0"
group :jekyll_plugins do
end
